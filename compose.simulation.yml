services:
  backend:
    environment:
      PLC_MODE: simulator
      PLC_HOST: simulator
      PLC_PORT: 1502
      CAMERA_STREAM_URL: http://simulator:8081/camera.mjpeg
  simulator:
    profiles: []
